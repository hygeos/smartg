
          ###############################
         # PARAMETRES DE LA SIMULATION #
        ###############################

# Nombre de photons a lancer (unsigned long long) (limite par le tableau du poids des photons en unsigned long long)
NBPHOTONS = 2000000000

# Nombre de boucles dans le kernel (unsigned int) (limite par watchdog, il faut tuer X)
NBLOOP = 18000

# Angle zenithal solaire en degrés (float)
THSDEG = 30.

# Graine avec laquelle on initialise les generateurs aleatoires (int)
	# si SEED est positif on l'utilise comme graine (cela permet d'avoir les memes nombres aleatoires d'une simulation a l'autre, et donc une meme simulation)
	# si SEED=-1 on crée une graine aleatoirement (donc toutes les simulations sont differentes les unes des autres)
SEED = -1

# Les threads sont rangés dans des blocks de taille XBLOCK*YBLOCK (limite par watchdog il faut tuer X + limite XBLOCK*YBLOCK =< 256 ou 512)
XBLOCK = 128
YBLOCK = 1
# et les blocks sont eux-même rangés dans un grid de taille XGRID*YGRID (limite par watchdog il faut tuer X + limite XGRID<65535 et YGRID<65535)
XGRID = 84
YGRID = 1

# Le demi-espace dans lequel on récupère les photons est divisé en boites
	# theta parcourt 0..PI
	# phi parcourt 0..2*PI
NBTHETA = 180
NBPHI = 180

# Longueur d'onde [nm] (float)
LAMBDA = 443.

# Epaisseur optique moleculaire (Rayley) (float)
TAURAY = 0.0533

# Epaisseur optique aerosol (float)
TAUAER = 0.0

# Albedo simple de diffusion des aerosols (float)
W0AER = 0.966883724738261

# Nombre d'échantillons pour le modèle de diffusion des aérosols (int)
LSAAER = 72001

#
NFAER = 1000000

# Type de profil atmospherique
	# 0 : profil standard avec echelle de hauteur
	# 3 : profil à 2 ou 3 couches (comme dans les SOS)
	# 2 : profil utilisateurs, Attention au bon format -> commencer directement les données sur la 1ère ligne
PROFIL = 0

# Echelle de hauteur aerosol [km] (utilise si PROFIL=0) (float)
HA = 2.0

# Echelle de hauteur moleculaire [km] (float)
HR = 8.0

# Altitude basse de la couche aerosol [km] (utilise si PROFIL=3) (float)
ZMIN = 0.

# Altitude haute de la couche aerosol [km] (utilise si PROFIL=3) (float)
ZMAX = 1.

# Vitesse du vent [m/s] (utilise si idioptre=1) (modele de Cox et Munk) (float)
WINDSPEED = 5.0

# Indice de refraction relatif air/eau (float)
NH2O = 1.33

# Type de simulation
	# -2 pour atmosphere seule
	# -1 pour dioptre seul
	# 0 pour ocean
	# 1 pour atmosphere et dioptre
	# 2 pour atmosphere dioptre et ocean
SIM =-2

# Type de reflexion de la surface (pour l'instant implémentation uniquement du cas 1)
	# 1 pour reflexion speculaire sur le dioptre
	# 2 pour reflexion diffuse de l'ocean
	# 3 pour les deux
SUR = 1

# Type de dioptre 
	# 0=plan
	# 1=agite avec reflexion multiple
	# 2=agite sans reflexion multiple
	# 3=surface lambertienne
DIOPTRE = 3

# Albedo simple de diffusion de la surface lambertienne (float)
W0LAM = 0.7

# Concentration en phytoplancton [mg.m-3] (utilise si SUR>=2) (float)
CONPHY = 0.1

# Forcage de la premiere diffusion (utilise si SIM = -2)
DIFFF = 1

# Chemin du fichier de sortie (Une grande partie du chemin est automatiquement créé afin de simplifier la classification des
# resultats)
PATHRESULTATSHDF = test

# Chemin du fichier témoin (Le .hdf et les détails de la simulation sont ajoutés par le programme automatiquement)
# PATHTEMOINHDF = validation/molecules_seules/simulation_27022012/tmp/

# Chemin du modèle de diffusion aérosol
PATHDIFFAER = /home/florent/MCCuda/fic/valid_T70.443

# Chemin du modèle atmosphérique utilisateur (utile uniquement si PROFIL = 2)
PATHPROFILATM = /home/florent/MCCuda/

